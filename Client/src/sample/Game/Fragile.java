package sample.Game;

import javafx.scene.paint.Color;

public class Fragile extends BoardElement {
    public Fragile(int x, int y, int width, int height) {
        super(x, y, width, height);
        this.color = Color.GOLDENROD;
    }
}
